import React from 'react';
import VehicleForm from './components/VehicleForm';  // Assuming VehicleForm.js is in the same directory

const App = () => {
  return (
    <div>
      <h1>Vehicle Form</h1>
      <VehicleForm />
    </div>
  );
};

export default App;